# 2026-04-01

- 우석 requested that whenever claw sends organized summaries/briefings, they should include not only the summary and insight but also claw's own opinion/judgment.
- Treat this as a standing style preference for future briefings.

## 브라우저 제어 복구 (2026-04-01)
- `gateway.nodes.browser.mode = "off"` 설정으로 node proxy 자동 라우팅 비활성화
- `channels.telegram.execApprovals.enabled = true` + approvers 설정으로 텔레그램 exec 승인 활성화
- `openclaw browser --browser-profile openclaw` 로 전용 브라우저 제어 복구 완료
- X (x.com) 로그인 상태 확인됨 → 9시 브리핑용 X 소스 수집 가능해짐
- 앞으로 브라우저 관련 명령은 텔레그램에서 승인 버튼으로 바로 처리 가능
