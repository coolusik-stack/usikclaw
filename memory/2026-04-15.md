
- 2026-04-15: open goal enforcement 규칙 보강. 목표가 열려 있는데 active worker가 0이면 idle이 아니라 failure로 취급. 워커 완료 이벤트가 오면 같은 턴에 보고 + active worker 0 점검 + 즉시 재발사를 강제한다. 이 규칙을 장기 목표 작업 기본값으로 사용.
- 2026-04-15: 우석은 다시 한 번 메인 채팅 응답 속도를 최우선으로 요구했다. 재발 방지로 메인 사용자 메시지 즉답 우선, 워커 결과 후처리 분리, 동시 active worker 1개 기본, 진행 보고는 짧고 빠르게 유지하기로 함.
- 2026-04-15: 장기 목표 워커 자동화 보강. 이제 워커는 종료 시 `next_best_lane`, `spawn_ready_task_brief`, `continue_if_goal_open`, `global_count_confidence`, `known_overlap_risk` 를 남겨야 하며, supervisor는 완료 후 복구보다 pre-handoff 승계를 우선한다.
- 2026-04-15: 중간 정지 보고 누락 재발 방지. `silence watchdog` 규칙 추가: open goal에서 20분 이상 워커 이벤트가 없으면 active worker / goal state / 최신 총계를 먼저 점검하고, active worker 0이면 즉시 사용자 보고 + same-turn relaunch를 실행한다.
